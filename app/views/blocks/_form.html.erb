<%= form_with(model: block) do |form| %>
  <% if block.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(block.errors.count, "error") %> prohibited this block from being saved:</h2>

      <ul>
        <% block.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :block_number %>
    <%= form.number_field :block_number %>
  </div>

  <div class="field">
    <%= form.label :number_of_rooms %>
    <%= form.number_field :number_of_rooms %>
  </div>

  <div class="field">
    <%= form.label :number_of_allotments %>
    <%= form.number_field :number_of_allotments %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
